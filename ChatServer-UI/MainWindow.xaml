<Window x:Class="ChatServer_UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatServer_UI"
        xmlns:controls="clr-namespace:ChatServer_UI.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <controls:ConversationListView Grid.Column="0"
            DataContext="{Binding ConversationList}"
            Background="LightBlue"/>

        <controls:ChatView Grid.Column="1"
            DataContext="{Binding ActiveChat}"
           Background="LightGreen"/>
    </Grid>
</Window>
