<UserControl x:Class="ChatServer_UI.Controls.ConversationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatServer_UI.Controls"
             xmlns:vm="clr-namespace:ChatServer_UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Grid.Column="0" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Search bar -->
            <RowDefinition Height="*"/>
            <!-- Chat list -->
        </Grid.RowDefinitions>

        <!-- Search -->
        <TextBox Grid.Row="0" Padding="8 4" VerticalContentAlignment="Center"
                 Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14"/>

        <!-- Chat list -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Conversations}"
                 SelectedItem="{Binding SelectedConversation}"
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ConversationViewModel}">
                    <Grid Height="64" Margin="8 4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Avatar circle -->
                        <Ellipse Grid.Column="0"
                                 Width="42" Height="42"
                                 VerticalAlignment="Center"/>

                        <!-- Name + preview -->
                        <StackPanel Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Margin="10 0 0 0">
                            <TextBlock Text="{Binding DisplayName}"
                                       FontWeight="SemiBold"
                                       FontSize="14"/>
                            <TextBlock Text="{Binding LastMessagePreview}"
                                       Foreground="Gray"
                                       FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>